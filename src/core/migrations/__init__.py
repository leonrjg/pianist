"""
Database migrations for the habit tracking application.

Migration files are numbered sequentially (001, 002, etc.) and each contains
an up() function to apply the migration and a down() function to rollback.
"""
